# -*- mode: python -*-
a = Analysis(['confidence_gui.py'],
             pathex=['/Users/matt/Code/envs/confidence-gui/confidence-gui'],
             hiddenimports=[],
             hookspath=None,
             runtime_hooks=None)
pyz = PYZ(a.pure)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='confidence_gui',
          debug=False,
          strip=None,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=None,
               upx=True,
               name='confidence_gui')
